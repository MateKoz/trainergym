import MainPage from "./navigation/MainPage";
import {BrowserRouter} from "react-router-dom";

const App = () => {
    return (
    <BrowserRouter>
        <MainPage/>
    </BrowserRouter>
)
};

export default App;
